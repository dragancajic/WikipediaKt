<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.eu5.learn_pisio.wikipediakt">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name">
            <!-- intent-filter says that this MainActivity is the launcher for our application -->
            <intent-filter>
                <!-- this activity, MainActivity, is the main action for opening our application -->
                <action android:name="android.intent.action.MAIN" />
                <!-- ... and, it has a LAUNCHER intent -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>